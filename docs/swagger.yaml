definitions:
  dto.APIResponse:
    properties:
      data: {}
      message:
        type: string
      status:
        type: string
    type: object
info:
  contact: {}
paths:
  /api/products:
    get:
      consumes:
      - application/json
      description: Get list of products
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/dto.APIResponse'
      summary: Get all products
      tags:
      - Products
swagger: "2.0"
